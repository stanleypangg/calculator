let a, op, b;

function assignNumberByID(target, id) {
    switch (id) {
        case ""
    }
}

const container = document.querySelector(".container");
container.addEventListener("click", (e) => {
    let id = e.target.id;
    if (op) assignNumberByID(b, id);
    else assignNumberByID(a, id);
});